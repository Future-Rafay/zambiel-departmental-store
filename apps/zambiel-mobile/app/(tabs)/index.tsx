import { useEffect, useState } from "react"; import { FlatList, Pressable, ScrollView, Text, View } from "react-native"; import { router } from "expo-router"; import { api } from "../../src/api"; import { useApp } from "../../src/app-state"; import { t } from "../../src/i18n"; import type { Category, Product } from "../../src/types"; import { Header, ProductCard, Screen, styles } from "../../src/ui"; import { colors, space } from "../../src/theme";
export default function HomeScreen() { const { locale, setLocale } = useApp(); const c=t(locale); const [products,setProducts]=useState<Product[]>([]); const [categories,setCategories]=useState<Category[]>([]); useEffect(()=>{ Promise.all([api.products(`locale=${locale}`),api.categories(locale)]).then(([p,x])=>{setProducts(p.slice(0,8));setCategories(x)}).catch(()=>undefined)},[locale]); return <Screen><Header title="Zambiel" right={<Pressable accessibilityRole="button" accessibilityLabel="Change language" onPress={()=>setLocale(locale==="de"?"en":"de")} style={{minWidth:48,minHeight:48,alignItems:"center",justifyContent:"center"}}><Text style={{fontFamily:"Inter_600SemiBold",color:colors.primary}}>{locale.toUpperCase()}</Text></Pressable>}/><FlatList data={products} numColumns={2} keyExtractor={(x)=>x.id} columnWrapperStyle={{gap:space.sm}} contentContainerStyle={{padding:space.md,gap:space.sm,paddingBottom:space.xl}} ListHeaderComponent={<View style={{gap:space.lg,marginBottom:space.lg}}><View style={{backgroundColor:colors.primary,borderRadius:20,padding:space.lg,borderBottomWidth:4,borderBottomColor:colors.accent}}><Text style={{fontFamily:"Archivo_700Bold",fontSize:30,lineHeight:36,color:colors.surface}}>{locale==="de"?"Für deinen Alltag. Aus der Schweiz.":"For everyday life. From Switzerland."}</Text></View><View><Text style={styles.title}>{c.categories}</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{gap:space.sm,paddingTop:12}}>{categories.map(x=><Pressable key={x.id} onPress={()=>router.push({pathname:"/(tabs)/shop",params:{category:x.slug}})} style={({pressed})=>({minHeight:48,justifyContent:"center",paddingHorizontal:16,borderRadius:24,backgroundColor:pressed?colors.border:colors.surface,borderWidth:1,borderColor:colors.border})}><Text style={{color:colors.text,fontFamily:"Inter_600SemiBold"}}>{x.name}</Text></Pressable>)}</ScrollView></View><Text style={styles.title}>{c.featured}</Text></View>} renderItem={({item})=><ProductCard product={item} onPress={()=>router.push(`/product/${item.slug}`)}/>}/></Screen> }

