import Link from "next/link";
import { AdminPage } from "@/components/admin/admin-ui";
const links = [{ href: "/admin/settings/general", title: "General", detail: "Identity, contact, announcement, and storefront defaults." }, { href: "/admin/settings/delivery-zones", title: "Delivery zones", detail: "Swiss postal codes, fees, minimums, and free-delivery thresholds." }, { href: "/admin/staff", title: "Staff", detail: "Owner-managed staff access." }, { href: "/admin/audit", title: "Audit history", detail: "Security and business mutation history." }];
export default function SettingsPage() { return <AdminPage title="Settings" description="Single-store operations and access."><div className="grid gap-4 sm:grid-cols-2">{links.map((item) => <Link key={item.href} href={item.href} className="rounded-xl border bg-white p-5 hover:border-primary"><h2 className="font-display text-lg font-bold">{item.title}</h2><p className="mt-1 text-sm text-muted">{item.detail}</p></Link>)}</div></AdminPage>; }
